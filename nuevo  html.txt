<!doctype html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>BioClinique · Medicina Integrativa en Bruselas</title>
  <meta name="description" content="Medicina integrativa, sueroterapia y acompañamiento personalizado por el Dr. Francisco José Fernández Pérez en Uccle, Bruselas." />
  
  <!-- SEO básico para redes sociales -->
  <meta property="og:title" content="BioClinique | Medicina Integrativa" />
  <meta property="og:description" content="Atención médica cercana y personalizada en Bruselas." />
  <meta property="og:type" content="website" />

  <style>
    :root{
      --bg:#fdfcfa; --bg-alt:#f2f4ee; --surface:#fff; --text:#20302b; --soft:#4b5a54;
      --teal:#0f6e63; --teal-dark:#0b4f47; --teal-pale:#e4efec; --red:#b4342e;
      --line:#ddd9cd; --shadow:0 14px 35px -18px rgba(32,48,43,.22); --radius:22px;
    }
    *{box-sizing:border-box} html{scroll-behavior:smooth}
    body{margin:0;background:var(--bg);color:var(--text);font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;line-height:1.6}
    
    /* Botón flotante WhatsApp */
    .whatsapp-float{position:fixed;bottom:25px;right:25px;background:#25D366;color:#fff;padding:15px 25px;border-radius:50px;font-weight:bold;z-index:999;box-shadow:0 4px 10px rgba(0,0,0,0.2);transition:transform .3s;}
    .whatsapp-float:hover{transform:scale(1.05);}

    /* Estilos base mejorados */
    .container{max-width:1100px;margin:0 auto;padding:0 20px}
    section{padding:80px 0}
    h1{font-size:clamp(2.5rem, 5vw, 3.5rem); color:var(--teal-dark); margin-bottom: 20px;}
    .btn{display:inline-block; padding:15px 30px; border-radius:50px; font-weight:700; text-align:center; transition:0.3s;}
    .btn-primary{background:var(--teal); color:#fff;}
    .btn-primary:hover{background:var(--teal-dark);}
    
    /* Grid de servicios */
    .grid-3{display:grid; grid-template-columns:repeat(auto-fit, minmax(300px, 1fr)); gap:20px; margin-top:40px;}
    .card{background:var(--surface); padding:30px; border-radius:var(--radius); border:1px solid var(--line); transition:0.3s;}
    .card:hover{border-color:var(--teal);}
    
    @media (max-width:768px){.hero-grid{grid-template-columns:1fr !important;}}
  </style>
</head>
<body>

<!-- Header -->
<header style="padding:20px 0; background:rgba(255,255,255,0.95); position:sticky; top:0; z-index:1000; border-bottom:1px solid var(--line);">
  <div class="container" style="display:flex; justify-content:space-between; align-items:center;">
    <strong>BioClinique</strong>
    <nav>
      <a href="#contacto" class="btn btn-primary" style="padding:10px 20px; font-size:0.9rem;">Reservar Cita</a>
    </nav>
  </div>
</header>

<!-- Hero -->
<section id="inicio">
  <div class="container">
    <h1>Medicina integrativa para tu bienestar.</h1>
    <p style="font-size:1.2rem; max-width:600px;">Plan personalizado de sueroterapia y acompañamiento clínico adaptado a tus necesidades reales.</p>
    <a href="#contacto" class="btn btn-primary">Empezar mi proceso</a>
  </div>
</section>

<!-- Contacto -->
<section id="contacto" class="alt">
  <div class="container">
    <h2>Contacto directo</h2>
    <p>Haz clic abajo para iniciar una consulta por WhatsApp:</p>
    <a href="https://wa.me/41779775917" class="btn btn-primary" target="_blank">Contactar por WhatsApp</a>
  </div>
</section>

<!-- Botón flotante -->
<a href="https://wa.me/41779775917" class="whatsapp-float" target="_blank">💬 WhatsApp</a>

</body>
</html>